# tools.py

# A simple greeting function
def greet(name):
    return f"Hello, {name}! Welcome back."

# A function to add two numbers
def add(a, b):
    return a + b

# A variable
version = "1.0"

# A list of supported users
users = ["Mzu", "Thabo", "Lerato"]

# A dictionary
config = {
    "debug": True,
    "theme": "dark"
}
