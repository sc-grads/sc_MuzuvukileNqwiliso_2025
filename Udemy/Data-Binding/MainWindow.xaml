<Window x:Class="Data_Binding.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Data_Binding"
        mc:Ignorable="d"
        Title="Data Binding" Height="200" Width="400">
    <Grid Height="134.04" Width="265">
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="1" Grid.Column="1" Margin="2" Content="Name"/>
        <TextBox Grid.Row="1" x:Name="Fullname" 
                 Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Column="2" Margin="2" TextAlignment="Left" />

        <Label Content="Age" Margin="2" Grid.Column="1" Grid.Row="2"/>
        <TextBox Grid.Column="2" x:Name="Age" 
                 Text="{Binding Age, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="2" Margin="2" TextAlignment="Left"/>

        <Button Grid.Column="1" Grid.Row="4"
                Content="Info"
                Click="Button_Click"
                Width="120" Margin="2"/>
        <Button Grid.Column="2" Grid.Row="4"
                Content="Show ListBox"
                Click="ShowListBoxWindow_Click"
                Width="120" Margin="2,2,2,2"/>
    </Grid>
</Window>
